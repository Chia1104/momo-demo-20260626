{
  "$schema": "https://turbo.build/schema.json",
  "tags": ["@repo/kv"],
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": [
        "CACHE_URI",
        "REDIS_URI",
        "VALKEY_URI",
        "POSTGRES_URI",
        "CACHE_PROVIDER"
      ],
      "passThroughEnv": [
        "CACHE_URI",
        "REDIS_URI",
        "VALKEY_URI",
        "POSTGRES_URI",
        "CACHE_PROVIDER"
      ]
    }
  }
}
