services:
  postgres:
    container_name: momo-demo-postgres
    image: pgvector/pgvector:pg18
    ports:
      - "5433:5432"
    volumes:
      - momo-demo-postgres:/var/lib/postgresql/data/pgdata
    env_file:
      - .env.global
    environment:
      - PGDATA=/var/lib/postgresql/data/pgdata
      - DEBUG=true

  redis:
    container_name: momo-demo-redis
    image: redis:alpine
    ports:
      - "6380:6379"
    volumes:
      - momo-demo-redis:/data

volumes:
  momo-demo-postgres:
  momo-demo-redis:
